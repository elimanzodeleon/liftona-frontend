import React from 'react';

// TODO this will be responsible for maintaing the posts of the users home page
// needed for when a user likes a post/unlikes a post/deletes a post
// we will have all home feed posts, cuurent users liked posts, and users posts
// we will pass all home feedd posts to workout list if any workout is in liked,
// we will render the liked button else unliked button
// when rendering we will check if workout is in liked or users to render respective like or delete button
// also create a reducer to control home page workouts state
const HomeContext = () => {
  return <div></div>;
};

export default HomeContext;
